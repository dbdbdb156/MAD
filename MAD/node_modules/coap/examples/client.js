const coap  = require('../') // or coap
    , req   = coap.request('coap://localhost/Matteo/ww')

req.on('response', function(res) {
	console.log(req);
	console.log('3. req.on(response)\n');
  res.pipe(process.stdout)
})

req.end()
